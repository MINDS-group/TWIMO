[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twimo"
version = "0.2.0"
description = "TWIMO: Driving-behavior Digital Twin experiments (HDD-like datasets)"
requires-python = ">=3.10"
